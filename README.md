# gulp-sass-es2015-test

GulpでSASSをトランスパイルするテスト

ついでにES2015の変換も入れてみた。Gulpの設定ファイルもES2015で書いた。

## 必要なもの
- Node.js

## 使い方
依存パッケージをインストールして、
```
$ npm install
```

Gulpタスクを実行すると、
```
$ npm run gulp
```
ブラウザに表示されます。
